# Use HTTPS for production. For a physical phone in development, replace localhost
# with the computer's LAN address (for example, http://192.168.1.20:3000).
EXPO_PUBLIC_CLIO_API_URL=http://localhost:3000
